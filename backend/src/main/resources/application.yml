server:
  port: ${PORT:8080}

spring:
  application:
    name: amogha-billing
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/amogha_billing}
      database: ${MONGODB_DB:amogha_billing}
      auto-index-creation: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

app:
  jwt:
    secret: ${JWT_SECRET:amogha-dev-secret-change-me-this-must-be-at-least-256-bits-long-value-x}
    ttl-minutes: 720
  otp:
    ttl-minutes: 5
  cors:
    # Comma-separated origins allowed to call the API. Defaults cover local dev (ng serve)
    # and the Netlify-hosted frontend; override with the CORS_ORIGINS env var if the host changes.
    allowed-origins: ${CORS_ORIGINS:http://localhost:4200,http://localhost:8088,https://amoghabilling.netlify.app}
  web:
    # Built Angular app (produced by `npm run build` in ../frontend). Served when present.
    static-dir: ${STATIC_DIR:../frontend/dist/amogha-billing/browser}

management:
  endpoints:
    web:
      exposure:
        include: health
